#include "queue.h"
#define SUCCESS 0
#define FAIL 1
#define DEPTH 32  // a fixed amount for this assignment

int dequeue(queue_t* queue, Packet_t* item) {
    return SUCCESS;
}

int enqueue(queue_t* queue, Packet_t* item) {
    return SUCCESS;
}